When working with data:
- Never include raw user IDs, email addresses, or PII in outputs
- Aggregate data before sharing (minimum group size of 5)
- Flag any dataset that might contain sensitive information before analyzing
- Use visitor_id and login_id for joins only — never surface them in reports
